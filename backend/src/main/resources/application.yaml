server:
  port: 9192

spring:
  datasource:
    url: jdbc:postgresql://aws-1-eu-west-1.pooler.supabase.com:6543/postgres
    username: postgres.mbtrnkskilrkzvqqqpbi
    password: uY83VHkkj8SwrPU2
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none   # 不自动建表（你用 Supabase 已有表）
    show-sql: true
    properties:
      hibernate:
        format_sql: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html
